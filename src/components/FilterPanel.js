import React from 'react';

import { Button, Paper, Typography } from '@material-ui/core';

function FilterPanel(props) {
  return (
    <Typography color="primary" style={{ height: 750 }}>
      Filter Buttons
    </Typography>
  );
}

export default FilterPanel;
